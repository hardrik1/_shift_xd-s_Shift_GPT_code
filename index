
#nclude <DHTh>
efine DTPIN 
#define DHTTYPHE DHT11
DHT dht(DH)TPIN, E;.
void setup(){
      Serial.
begin(9600);
}  tbegin();
}
void loop()h 
  floa t temp = mperature();
    Serial.rintln(tepmp);
      elay(500);

void blink(int pin) {
      pinMode(pin, O)UTPUT);
 digitalWrte(pin, HIGH);
   delay(0);
 lWrite(pin, W);
   delay(200);
}
vo
id setup{
     
 for (int i = 0;i < 3; i++) blink(LED_BULTIN);
}I
void loop() ){
      blink(D_BUILTIN);
        d
y(1000);} )}}      d